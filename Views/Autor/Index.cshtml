@model AutorViewModel

@{
    ViewData["Title"] = "Index";
}
<body class="profesorFondo">
<h1 class="container display-4 rounded" style="color: white; text-align: center font-weight: bold;">Autor ISTEA</h1>
<br>
<p>
    <a asp-action="Create" class="btn btn-light">Crear nuevo autor</a>
</p>
<br>
<div class="row">
<form method="get" asp-action="Index" asp-controller="Autor">
    <div class="col-6 form-group">
        <input class="form-control" asp-for="NameFilter"/>
    </div>
    <br>
    <button class="btn btn-light" type="submit">Buscar</button>
</form>
</div>
<br>
<table class="table table-dark table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Autors[0].Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autors[0].Apellido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autors[0].Edad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autors[0].Genero)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Autors) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Edad)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Genero)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-outline-success btn-sm">Editar</a>
                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-info btn-sm">Detalles</a>
                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger btn-sm">Eliminar</a>
            </td>
        </tr>
}
    </tbody>
</table>
<br>
<div class="tirar-derecha">
        <a href="@Url.Action("Index", "Profesor")" class="btn btn-light btn-sm">
            <img src="~/Images/arrow-left-circle.png" class="pruebatamaño" />Volver a profesor
        </a>
        </div>
</body>
